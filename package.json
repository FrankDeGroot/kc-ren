{
  "devDependencies": {
    "@types/chai": "^4.0.10",
    "@types/mocha": "^2.2.44",
    "@types/node": "^8.5.1",
    "chai": "^4.1.2",
    "mocha": "^4.0.1",
    "npm-run-all": "^4.1.2",
    "rimraf": "^2.6.2"
  },
  "scripts": {
    "build": "tsc -t es2017 -m commonjs src/*.ts test/*.ts",
    "clean": "rimraf **/*.js",
    "test": "mocha --recursive",
    "verify": "run-s clean build test"
  },
  "dependencies": {}
}
